# Local Development Environment Variables
# Copy this file to .env.local and update with your actual values

NODE_ENV=development

# Mailjet Configuration (Optional for testing)
# Get these from https://app.mailjet.com/account/api_keys
MJ_API_KEY=your_mailjet_api_key_here
MJ_API_SECRET=your_mailjet_api_secret_here

# Email Configuration
CONTACT_EMAIL=info@vsptech.com
FROM_EMAIL=noreply@vsptech.com

# Note: If Mailjet credentials are not provided, the contact form will still work
# but will return a mock success response instead of sending actual emails
